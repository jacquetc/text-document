# Generated by Qleany v1.5.1 from feature_cargo.tera
[package]
name = "text-document-io"
authors.workspace = true
documentation.workspace = true
description = "Import/export for text-document: plain text, Markdown, HTML, LaTeX, DOCX"
keywords.workspace = true
categories.workspace = true
version.workspace = true
edition.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
readme = "../../README.md"
publish = true

[dependencies]
anyhow = { workspace = true }
common = { workspace = true }
macros = { workspace = true }
serde = "1.0"
serde_json = "1.0"
chrono = { workspace = true }
uuid = { workspace = true }
regex = "1"
docx-rs = "0.4"

[dev-dependencies]
test_harness = { workspace = true }

[features]
default = []
# Pass-through for test code that needs to query the rope/block_offsets
# fields that only exist on RopeStore. Equivalent to enabling
# text-document-common/rope_backend; this lets per-crate tests
# `#[cfg(feature = "rope_backend")]` work without a top-level
# `--features text-document-common/rope_backend` invocation.
rope_backend = ["common/rope_backend"]

[lints]
workspace = true



