// Generated by Qleany v1.4.8 from frontend_entity_commands.tera

//! Document entity commands
#![allow(unused_imports, dead_code)]

use crate::app_context::AppContext;
use anyhow::{Context, Result};
use common::direct_access::document::DocumentRelationshipField;
use common::types::EntityId;
use direct_access::DocumentRelationshipDto;
use direct_access::{CreateDocumentDto, DocumentDto, document_controller};

/// Create a new document entity (orphan, no parent)
pub fn create_orphan_document(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateDocumentDto,
) -> Result<DocumentDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::create_orphan(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("creating document")
}
/// Create a new document entity as child of owner
pub fn create_document(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &CreateDocumentDto,
    owner_id: EntityId,
    index: i32,
) -> Result<DocumentDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::create(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
        owner_id,
        index,
    )
    .context("creating document")
}
/// Create multiple document entities (orphan, no parent)
pub fn create_orphan_document_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateDocumentDto],
) -> Result<Vec<DocumentDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::create_orphan_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("creating document entities")
}
/// Create multiple document entities as children of owner
pub fn create_document_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[CreateDocumentDto],
    owner_id: EntityId,
    index: i32,
) -> Result<Vec<DocumentDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::create_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
        owner_id,
        index,
    )
    .context("creating document entities")
}
/// Get a document entity by ID
pub fn get_document(ctx: &AppContext, id: &EntityId) -> Result<Option<DocumentDto>> {
    document_controller::get(&ctx.db_context, id).context("getting document")
}

/// Get multiple document entities by IDs
pub fn get_document_multi(ctx: &AppContext, ids: &[EntityId]) -> Result<Vec<Option<DocumentDto>>> {
    document_controller::get_multi(&ctx.db_context, ids).context("getting document entities")
}

/// Get all document entities.
/// Note: returns entities in database key order (by EntityId), not insertion order
/// or any user-defined sort. For ordered collections, use relationship-based
/// retrieval (e.g. get_*_relationship for ordered_one_to_many fields).
pub fn get_all_document(ctx: &AppContext) -> Result<Vec<DocumentDto>> {
    document_controller::get_all(&ctx.db_context).context("getting all document entities")
}

/// Update a document entity
pub fn update_document(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &DocumentDto,
) -> Result<DocumentDto> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::update(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("updating document")
}

/// Update multiple document entities
pub fn update_document_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dtos: &[DocumentDto],
) -> Result<Vec<DocumentDto>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::update_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dtos,
    )
    .context("updating document entities")
}

/// Remove a document entity by ID
pub fn remove_document(ctx: &AppContext, stack_id: Option<u64>, id: &EntityId) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::remove(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
    )
    .context("removing document")
}

/// Remove multiple document entities by IDs
pub fn remove_document_multi(
    ctx: &AppContext,
    stack_id: Option<u64>,
    ids: &[EntityId],
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::remove_multi(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        ids,
    )
    .context("removing document entities")
}

/// Get a document relationship
pub fn get_document_relationship(
    ctx: &AppContext,
    id: &EntityId,
    field: &DocumentRelationshipField,
) -> Result<Vec<EntityId>> {
    document_controller::get_relationship(&ctx.db_context, id, field)
        .context("getting document relationship")
}

/// Get relationship IDs for multiple document entities at once
pub fn get_document_relationship_many(
    ctx: &AppContext,
    ids: &[EntityId],
    field: &DocumentRelationshipField,
) -> Result<std::collections::HashMap<EntityId, Vec<EntityId>>> {
    document_controller::get_relationship_many(&ctx.db_context, ids, field)
        .context("getting document relationships (many)")
}

/// Get relationship count for a document entity
pub fn get_document_relationship_count(
    ctx: &AppContext,
    id: &EntityId,
    field: &DocumentRelationshipField,
) -> Result<usize> {
    document_controller::get_relationship_count(&ctx.db_context, id, field)
        .context("getting document relationship count")
}

/// Get relationship IDs for a document entity with pagination
pub fn get_document_relationship_in_range(
    ctx: &AppContext,
    id: &EntityId,
    field: &DocumentRelationshipField,
    offset: usize,
    limit: usize,
) -> Result<Vec<EntityId>> {
    document_controller::get_relationship_in_range(&ctx.db_context, id, field, offset, limit)
        .context("getting document relationship range")
}

/// Set a document relationship
pub fn set_document_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    dto: &DocumentRelationshipDto,
) -> Result<()> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::set_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        dto,
    )
    .context("setting document relationship")
}

/// Move (reorder) IDs within a document relationship
pub fn move_document_relationship(
    ctx: &AppContext,
    stack_id: Option<u64>,
    id: &EntityId,
    field: &DocumentRelationshipField,
    ids_to_move: &[EntityId],
    new_index: i32,
) -> Result<Vec<EntityId>> {
    let mut undo_redo_manager = ctx.undo_redo_manager.lock().unwrap();
    document_controller::move_relationship(
        &ctx.db_context,
        &ctx.event_hub,
        &mut undo_redo_manager,
        stack_id,
        id,
        field,
        ids_to_move,
        new_index,
    )
    .context("moving document relationship")
}
