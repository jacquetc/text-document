// Generated by Qleany v1.4.8 from feature_use_case.tera
use crate::SetFrameFormatDto;
use anyhow::{Result, anyhow};
use common::database::CommandUnitOfWork;
use common::entities::{Document, Frame};
use common::types::EntityId;

pub trait SetFrameFormatUnitOfWorkFactoryTrait: Send + Sync {
    fn create(&self) -> Box<dyn SetFrameFormatUnitOfWorkTrait>;
}

//TODO: adapt entities and actions to real use :
// Create, CreateMulti, Get, GetMulti, Update, UpdateMulti, remove,
// removeMulti, GetRelationship, GetRelationshipsFromRightIds,
// SetRelationship, SetRelationshipMulti
//
// You have here a read-write unit of work trait.
//
// RO means Read Only.
// Do not mix read-only and write actions in the same unit of work.
//
// Exactly the same macros must be set in the use case uow trait file in ../units_of_work/set_frame_format_uow.rs
//
#[macros::uow_action(entity = "Document", action = "Get")]
#[macros::uow_action(entity = "Document", action = "GetMulti")]
#[macros::uow_action(entity = "Document", action = "Snapshot")]
#[macros::uow_action(entity = "Document", action = "Restore")]
#[macros::uow_action(entity = "Frame", action = "Get")]
#[macros::uow_action(entity = "Frame", action = "GetMulti")]
#[macros::uow_action(entity = "Frame", action = "Snapshot")]
#[macros::uow_action(entity = "Frame", action = "Restore")]
pub trait SetFrameFormatUnitOfWorkTrait: CommandUnitOfWork {}

pub struct SetFrameFormatUseCase {
    uow_factory: Box<dyn SetFrameFormatUnitOfWorkFactoryTrait>,
}

impl SetFrameFormatUseCase {
    pub fn new(uow_factory: Box<dyn SetFrameFormatUnitOfWorkFactoryTrait>) -> Self {
        SetFrameFormatUseCase { uow_factory }
    }

    pub fn execute(&mut self, dto: &SetFrameFormatDto) -> Result<()> {
        let mut uow = self.uow_factory.create();
        uow.begin_transaction()?;

        //TODO: SetFrameFormatUseCase to be implemented
        unimplemented!("SetFrameFormatUseCase unimplemented");
        uow.commit()?;
        Ok(())
    }
}
use common::undo_redo::UndoRedoCommand;
use std::any::Any;
impl UndoRedoCommand for SetFrameFormatUseCase {
    fn undo(&mut self) -> Result<()> {
        //TODO: SetFrameFormatUseCase  undo to be implemented
        unimplemented!("SetFrameFormatUseCase undo unimplemented");

        Ok(())
    }

    fn redo(&mut self) -> Result<()> {
        //TODO: SetFrameFormatUseCase  redo to be implemented
        unimplemented!("SetFrameFormatUseCase redo unimplemented");

        Ok(())
    }
    fn as_any(&self) -> &dyn Any {
        self
    }
}
