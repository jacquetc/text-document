// Generated by Qleany v1.7.3 from db_context.tera
// Phase 2: hand-edited to allow `Store` to be cfg-swapped between
// HashMapStore (default) and RopeStore (under `rope_backend`).

use crate::database::Store;

use std::sync::Arc;

#[derive(Clone, Debug)]
pub struct DbContext {
    store: Arc<Store>,
}

impl DbContext {
    pub fn new() -> Result<Self, crate::error::RepositoryError> {
        Ok(DbContext {
            store: Arc::new(Store::default()),
        })
    }

    pub fn get_store(&self) -> &Arc<Store> {
        &self.store
    }
}
