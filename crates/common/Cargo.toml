# Generated by Qleany v1.4.8 from common_cargo.tera

[package]
name = "text-document-common"
authors.workspace = true
documentation.workspace = true
description = "Shared entities, database, events, and undo/redo infrastructure for text-document"
keywords.workspace = true
categories.workspace = true
version.workspace = true
edition.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true
readme = "../../README.md"
publish = true

[lib]
name = "common"

[dependencies]
thiserror = { workspace = true }
serde = { version = "1.0", features = ["derive"] }
im = { version = "15.1.0", features = ["serde"] }
anyhow = { workspace = true }
flume = { workspace = true }
serde_json = "1.0"
chrono = { workspace = true }
uuid = { workspace = true }
log = "0.4"
pulldown-cmark = "0.13.1"
scraper = "0.26.0"
ego-tree = "0.11.0"
ropey = "1.6"

[features]
default = []
# Phase 2 internal scaffolding: routes DbContext and the Store type
# alias through RopeStore instead of HashMapStore. The RopeStore type
# itself is always compiled (gating only the wiring lets tests for
# the new data structures run regardless of feature state). The gate
# goes away at the end of Phase 2 (§8.3) once RopeStore is the only
# backend.
rope_backend = []

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
